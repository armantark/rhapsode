# Project Brief

Rhapsode is a local-first, single-user oral memorization platform for exact
spoken recall across languages and scripts. It combines structured passages,
optional linguistic annotations, reference audio, guided oral practice modes,
and durable spaced repetition.

The backend is Python/FastAPI. The frontend will be SvelteKit and is deliberately
implemented in a separate thread against the generated OpenAPI contract.

